// Tessera/HostEntryView.swift
//
// The v1 HostEntryView form that lived here was replaced by HostDetailView
// (see the note at the top of HostDetailView.swift) and nothing instantiated
// it anymore; its styling also predated the Typography/DesignTokens system.
// The file itself stays because the Xcode project references sources by name
// (no filesystem-synced groups) — it is intentionally empty.
