# GitHub Pages config — publish the site from this /docs folder
# (repo Settings → Pages → Deploy from a branch → main, /docs).
title: Tessera
description: SSH, Mosh, and tmux terminal for iPad
theme: jekyll-theme-primer
